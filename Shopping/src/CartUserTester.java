public class CartUserTester {
    public static void main(String[] args) {
        /*System.out.println("AN ADMIN IS ABOUT TO PUT FOR YOU A CATALOGUE OF ALL AVAILABLE PRODUCTS  , OPERATION MAY TAKE SOME TIME : ");
        Admin a=new Admin("admin","admin123");
        a.productsCatalogue();
        int x=3;
        List<Product> liste =a.catalogue; // products available on the app
        ShoppingCart cart=new ShoppingCart(liste);

        for (Product p:liste) {
           Scanner s=new Scanner(System.in);
           System.out.print("select quantity of  [ "+ p.getName() + " ]  to add to your cart : ");
           x=s.nextInt();
            cart.add_item(p, x);
        }
        cart.displayShoppingCart();
        InventoryManager i=new InventoryManager();
        /*i.checkProductsStock(liste,cart);
        cart.displayShoppingCart();*/
       /* User Rayen = new User("rayen_26", "Ray0#_pre");
        Order o=new Order(liste.get(0),cart.getShoppingCart().get(liste.get(0)),Rayen.getUserid(),cart);
        o.orderDisplay(liste);
        o.getOrderDetails();
        for (Product e:liste) System.out.println("product : " + e.getName() + " -------   available quantity : " + e.getAvailable_quantity());
        Rayen.rateProduct(liste.get(0));*/


    }
}